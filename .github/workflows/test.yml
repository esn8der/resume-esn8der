name: CI/CD para NextJS

on: [pull_request]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/restore-cache@v3
        with:
          key: ${{ runner.os }}-yarn-${{ hashFiles('.yarnrc.yml') }}
          path: ~/.cache/yarn
      - name: Install dependencies
        run: yarn install
      - name: Lint
        run: yarn lint --quiet
